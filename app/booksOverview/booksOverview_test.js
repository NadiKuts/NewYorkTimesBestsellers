'use strict';

describe('myApp.booksOverview module', function() {

  beforeEach(module('myApp.booksOverview'));

  describe('booksOverview controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var booksOverviewCtrl = $controller('booksOverviewCtrl');
      expect(booksOverviewCtrl).toBeDefined();
    }));

  });
});